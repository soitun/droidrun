[project]
name = "droidrun"
version = "0.5.8"
description = "Compatibility shim — droidrun has been renamed to mobilerun"
requires-python = ">=3.11,<3.14"
dependencies = ["mobilerun==0.5.8"]

[project.optional-dependencies]
anthropic = ["mobilerun[anthropic]==0.5.8"]
deepseek = ["mobilerun[deepseek]==0.5.8"]
langfuse = ["mobilerun[langfuse]==0.5.8"]
dev = ["mobilerun[dev]==0.5.8"]

[project.scripts]
droidrun = "droidrun.cli_shim:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["droidrun"]
